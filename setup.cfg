[metadata]
name = schemagate
version = 0.1.0
description = Schema evolution compatibility gate (backward / forward / full)

[options]
packages = schemagate
python_requires = >=3.8
include_package_data = True

[options.package_data]
schemagate = static/*.html

[options.extras_require]
test =
    pytest
